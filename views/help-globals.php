<?php
/**
 * Globals tab content in admin help.
 *
 * @package WpIgnitor
 * @since 1.0.0
 */
?>
<h3><?= __( 'About Globals Tab', IGNITOR ) ?></h3>
<p>
  <?= __( "This settings section contains settings that have a destructive impact on the entire website. It's risky to do, so it's a good idea to do it before you bring in the content.", IGNITOR ) ?><br>
  <br>
  <?= __( 'By installing WordPress to a subdirectory instead of the document root, you can prevent malicious users from accessing the attack vector. With WP Ignitor you can easily change the installation path.', IGNITOR ) ?><br>
  <br>
  <?= __( 'There are many important things that can be controlled by the WordPress configuration file "wp-config.php", but you need to update the file directly. WP Ignitor can be updated from this section with a simple operation.', IGNITOR ) ?><br>
  <br>
  <?= __( 'Maintaining ".htaccess", which controls the routing of WordPress sites, requires specialized knowledge and can be a pain. WP Ignitor will take over your efforts.', IGNITOR ) ?><br>
</p>
